server:
  port: 8086
  tomcat:
    uri-encoding: UTF-8
  servlet:
    session:
      timeout: 10
spring:
  thymeleaf:
    prefix: classpath:/views/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false
